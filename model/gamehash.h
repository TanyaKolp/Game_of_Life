#ifndef GAMEHASH_H
#define GAMEHASH_H

//#include "game.h"	// A.K. функциональность данного include не используетс€

/*	
// A.K.  ласс, который нигде не используетс€, следуеть исключать из
// исходников, либо из компил€ции (в pro-файле), либо, на худой конец,
// хот€ бы следует закомментировать
class GameHash
{
public:
    GameHash();

    // Game interface
public:
    void clearGame();
    QList<QPoint> computeNextGeneration();
};
*/

#endif // GAMEHASH_H
