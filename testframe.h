#ifndef TESTFRAME_H
#define TESTFRAME_H

#include <QFrame>
#include <QWidget>

/*	// A.K. Код, который не используется, следует комментировать
class TestFrame : public QFrame
{
    Q_OBJECT
public:
    explicit TestFrame(QWidget *parent = 0);

signals:

public slots:

    // QWidget interface
protected:
    void paintEvent(QPaintEvent*);
};
*/
#endif // TESTFRAME_H
